spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: chatbot
  autoconfigure:
    exclude:
      - org.springframework.ai.model.anthropic.autoconfigure.AnthropicChatAutoConfiguration

  # Configure OpenRouter API key (replace with your actual API key)
  ai:
    openai:
      api-key: ${ANTHROPIC_API_KEY}
      base-url: https://openrouter.ai/api

      # Specify the model you want to use (e.g., a free one, check the OpenRouter site for options)
      chat:
        options:
          model: openai/gpt-oss-20b

    #needed to disable spring AI's autoconfigure to anthropic.
    model:
      chat: openai
logging:
  level:
    root: INFO

USER_NAME: ${USER_NAME}
USER_PASSWORD: $(USER_PASSWORD)

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

server:
  port: 8080



